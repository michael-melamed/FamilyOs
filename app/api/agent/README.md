# `agent` API Sub-routes
## Purpose
Houses proxy endpoints dynamically resolving external requests for Claude integrations if HTTP fallback is required explicitly.
## Keys Files
- `route.ts` - Agent command executor proxy.
## Dependencies
Anthropic SDK
